/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = {
  env: {
    //decide on naming    
    titleB: 'Cthonic Elysium',
    title: 'Infernal Dimension',
    //likely both, two rows
    subtitle1: 'Environment Art & Level Design',
    subtitle2: 'Projects & Services',
  },
}
